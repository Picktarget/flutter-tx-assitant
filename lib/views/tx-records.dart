import 'package:flutter/material.dart';

class TxRecords extends StatelessWidget {
  const TxRecords({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: new Center(child: Text('this is tx records page')),
    );
  }
}

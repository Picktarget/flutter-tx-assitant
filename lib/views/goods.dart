import 'package:flutter/material.dart';

class Goods extends StatelessWidget {
  const Goods({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Center(child: Text('this is goods page')),
    );
  }
}
